name: Test

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  typecheck:
    name: Typecheck ratchet
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      # Ratchet, not a gate (#327). The repo has a pre-existing type-error
      # backlog recorded in `.tsc-baseline`; this fails only if the count
      # GOES UP, so new type errors cannot be introduced while the backlog is
      # paid down separately. A typecheck step would have caught #329's
      # `dedupeKey is not defined` in April and #330's object-into-a-string
      # argument at author time.
      - name: Typecheck (no new type errors)
        run: bun run typecheck

  test:
    name: Bun test (module suites)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      # Scoped to the suites that pass cleanly. `bun test` treats these as path
      # patterns, so each must point at a dir that actually holds *.test.ts:
      # the module dirs (src/bus, src/tuner, src/observability, src/skills-tuner)
      # plus the governance tests — which live under src/__tests__/governance,
      # NOT src/governance (that dir has no tests). The rest of the root
      # `src/__tests__/` suite is intentionally excluded for now: ~41 pre-existing
      # failures, a mix of cross-test pollution (pass in isolation, fail in the
      # shared process) and infra-dependent tests (DB/queue CI doesn't provide).
      # Folding those in would make CI permanently red. Tracked for cleanup — see
      # the issue linked in the PR — after which these paths widen toward full.
      # `src/__tests__/event-processor.test.ts` added #304 cluster 2: was
      # 4 pass / 9 fail on a source-side ReferenceError; now 14 pass, stable
      # 8/8 across repeat runs.
      #
      # `src/__tests__/mcp-multiplexer-integration.test.ts` added #304: first
      # file reclaimed from the excluded root suite (was 0 pass / 7 fail on a
      # stale settings fixture; now 7 pass, verified 6/6 across repeat runs).
      # Its sibling `session-persistence-integration.test.ts` is fixed too but
      # deliberately NOT added yet — it carries a load-sensitive flake tracked
      # in #326, and adding it would make CI randomly red.
      #
      # `src/adapters` added #301: the adapter suites are clean (252 pass) and
      # they are where outbound routing and turn-semantics risk actually lives.
      # Without them the adapter half of a delivery change never runs in CI —
      # and there is no typecheck step to back it up.
      - name: Run tests (clean suites)
        run: bun test src/bus src/adapters src/tuner src/observability src/skills-tuner src/plugins/eval-framework src/__tests__/governance src/__tests__/mcp-multiplexer-integration.test.ts src/__tests__/event-processor.test.ts
